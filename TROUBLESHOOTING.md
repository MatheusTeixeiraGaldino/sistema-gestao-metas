# 🆘 Guia de Troubleshooting\n\nSoluções para problemas comuns durante o setup.\n\n---\n\n## ❌ Problema: \"command not found: node\"\n\n### Causa\nNode.js não foi instalado ou não está no PATH.\n\n### Solução\n\n**Windows:**\n1. Vá para [https://nodejs.org](https://nodejs.org)\n2. Baixe a versão LTS (20.x)\n3. Execute o instalador\n4. **Importante**: Marque \"Add to PATH\"\n5. Reinicie o computador\n6. Abra novo PowerShell e teste: `node --version`\n\n**macOS:**\n```bash\nbrew install node\nnode --version\n```\n\n**Linux:**\n```bash\nsudo apt update\nsudo apt install nodejs npm\nnode --version\n```\n\n---\n\n## ❌ Problema: \"command not found: git\"\n\n### Causa\nGit não foi instalado.\n\n### Solução\n\n**Windows:**\n1. Vá para [https://git-scm.com/download/win](https://git-scm.com/download/win)\n2. Execute o instalador\n3. Escolha as opções padrão\n4. Reinicie o computador\n5. Teste: `git --version`\n\n**macOS:**\n```bash\nbrew install git\ngit --version\n```\n\n**Linux:**\n```bash\nsudo apt install git\ngit --version\n```\n\n---\n\n## ❌ Problema: \"Permission denied (publickey)\"\n\n### Causa\nChave SSH não está configurada ou não foi adicionada ao GitHub.\n\n### Solução\n\n#### Passo 1: Gerar Chave SSH\n\n```bash\nssh-keygen -t ed25519 -C \"seu-email@example.com\"\n```\n\nPressione Enter 3 vezes (valores padrão).\n\n#### Passo 2: Copiar Chave Pública\n\n**macOS/Linux:**\n```bash\ncat ~/.ssh/id_ed25519.pub\n```\n\n**Windows PowerShell:**\n```bash\nGet-Content $env:USERPROFILE\\.ssh\\id_ed25519.pub\n```\n\nCopie tudo que aparecer.\n\n#### Passo 3: Adicionar no GitHub\n\n1. Vá para [https://github.com/settings/keys](https://github.com/settings/keys)\n2. Clique em \"New SSH key\"\n3. Cole a chave\n4. Clique em \"Add SSH key\"\n\n#### Passo 4: Testar\n\n```bash\nssh -T git@github.com\n```\n\nVocê deve ver: \"Hi seu-usuario! You've successfully authenticated\"\n\n---\n\n## ❌ Problema: \"fatal: could not read Username\"\n\n### Causa\nGit não está configurado com seu nome e email.\n\n### Solução\n\n```bash\ngit config --global user.name \"Seu Nome Completo\"\ngit config --global user.email \"seu-email@example.com\"\n```\n\nVerifique:\n```bash\ngit config --global user.name\ngit config --global user.email\n```\n\n---\n\n## ❌ Problema: \"fatal: remote origin already exists\"\n\n### Causa\nVocê já tem um remote configurado.\n\n### Solução\n\n```bash\n# Remover remote existente\ngit remote remove origin\n\n# Adicionar novo remote\ngit remote add origin https://github.com/seu-usuario/metas-management-system.git\n\n# Verificar\ngit remote -v\n```\n\n---\n\n## ❌ Problema: \"fatal: The current branch main has no upstream branch\"\n\n### Causa\nVocê está tentando fazer push sem ter configurado o upstream.\n\n### Solução\n\n```bash\ngit push -u origin main\n```\n\nOu, se você está em outra branch:\n\n```bash\ngit push -u origin nome-da-branch\n```\n\n---\n\n## ❌ Problema: \"npm ERR! code ENOENT\"\n\n### Causa\nArquivo `package.json` não encontrado ou você não está no diretório correto.\n\n### Solução\n\n```bash\n# Verificar se você está no diretório correto\npwd  # macOS/Linux\ncd   # Windows PowerShell\n\n# Deve mostrar algo como: /home/usuario/metas-management-system\n\n# Se não estiver, navegue para lá\ncd caminho/para/metas-management-system\n\n# Verifique se package.json existe\nls package.json  # macOS/Linux\ndir package.json # Windows PowerShell\n```\n\n---\n\n## ❌ Problema: \"pnpm: command not found\"\n\n### Causa\npnpm não foi instalado.\n\n### Solução\n\n```bash\nnpm install -g pnpm\n\n# Verificar\npnpm --version\n```\n\n---\n\n## ❌ Problema: \"error: failed to push some refs to 'origin'\"\n\n### Causa\nSeu repositório local está desatualizado.\n\n### Solução\n\n```bash\n# Atualizar seu repositório local\ngit pull origin main\n\n# Tentar push novamente\ngit push origin main\n```\n\nSe ainda não funcionar:\n\n```bash\n# Forçar push (cuidado!)\ngit push -f origin main\n```\n\n---\n\n## ❌ Problema: \"ECONNREFUSED\" ao conectar no banco\n\n### Causa\nBanco de dados não está rodando ou a string de conexão está errada.\n\n### Solução\n\n#### Se usar Docker Compose:\n\n```bash\n# Verificar se Docker está rodando\ndocker-compose ps\n\n# Se não estiver, inicie\ndocker-compose up -d\n\n# Verificar logs\ndocker-compose logs mysql\n```\n\n#### Se usar PlanetScale:\n\n1. Verifique a string de conexão em [https://app.planetscale.com](https://app.planetscale.com)\n2. Copie a string correta\n3. Atualize em GitHub Secrets\n4. Redeploy na Vercel\n\n#### Se usar MySQL local:\n\n```bash\n# Verificar se MySQL está rodando\nmysql -u root -p\n\n# Se não conseguir conectar, reinicie o serviço\n# macOS\nbrew services restart mysql\n\n# Linux\nsudo systemctl restart mysql\n\n# Windows\n# Use MySQL Workbench ou reinicie o serviço via Services\n```\n\n---\n\n## ❌ Problema: \"Port 3000 already in use\"\n\n### Causa\nOutra aplicação está usando a porta 3000.\n\n### Solução\n\n#### Opção 1: Usar porta diferente\n\n```bash\nPORT=3001 pnpm dev\n```\n\nAcesse `http://localhost:3001`\n\n#### Opção 2: Matar processo na porta 3000\n\n**macOS/Linux:**\n```bash\nlsof -i :3000\n# Copie o PID (número na coluna PID)\nkill -9 <PID>\n```\n\n**Windows PowerShell:**\n```bash\nGet-Process -Id (Get-NetTCPConnection -LocalPort 3000).OwningProcess | Stop-Process\n```\n\n---\n\n## ❌ Problema: \"Module not found: Can't resolve '@/components/ui/button'\"\n\n### Causa\nDependências não foram instaladas corretamente.\n\n### Solução\n\n```bash\n# Limpar cache\nrm -rf node_modules pnpm-lock.yaml\n\n# Reinstalar\npnpm install\n\n# Tentar novamente\npnpm dev\n```\n\n---\n\n## ❌ Problema: \"Build failed\" na Vercel\n\n### Causa\nErro durante o build em produção.\n\n### Solução\n\n#### Passo 1: Verificar logs\n\n1. Vá para [https://vercel.com/dashboard](https://vercel.com/dashboard)\n2. Clique no seu projeto\n3. Clique em \"Deployments\"\n4. Clique no deployment que falhou\n5. Vá para \"Build Logs\"\n6. Procure pela mensagem de erro\n\n#### Passo 2: Testar localmente\n\n```bash\npnpm build\n```\n\nSe falhar localmente, o erro está no seu código.\n\n#### Passo 3: Verificar variáveis de ambiente\n\n1. Vá para Settings do projeto na Vercel\n2. Clique em \"Environment Variables\"\n3. Verifique se todas as variáveis estão configuradas\n4. Especialmente: `DATABASE_URL`, `JWT_SECRET`\n\n#### Passo 4: Fazer novo deploy\n\n```bash\ngit add .\ngit commit -m \"Fix build\"\ngit push origin main\n```\n\n---\n\n## ❌ Problema: \"Database migration failed\"\n\n### Causa\nSchema do banco está inconsistente.\n\n### Solução\n\n```bash\n# Verificar schema\ncat drizzle/schema.ts\n\n# Listar migrações\nls drizzle/*.sql\n\n# Se houver erro, recriar migrações\nrm drizzle/*.sql\npnpm db:push\n```\n\nSe ainda não funcionar, consulte os logs:\n\n```bash\ndrizzle-kit generate --verbose\n```\n\n---\n\n## ❌ Problema: \"Firebase authentication failed\"\n\n### Causa\nCredenciais do Firebase estão incorretas ou não configuradas.\n\n### Solução\n\n1. Vá para [https://console.firebase.google.com](https://console.firebase.google.com)\n2. Clique no seu projeto\n3. Clique em ⚙️ (Settings)\n4. Vá para \"Project settings\"\n5. Copie as credenciais\n6. Atualize em `.env.local`:\n\n```env\nVITE_FIREBASE_API_KEY=seu_api_key\nVITE_FIREBASE_AUTH_DOMAIN=seu_auth_domain\nVITE_FIREBASE_PROJECT_ID=seu_project_id\nVITE_FIREBASE_STORAGE_BUCKET=seu_storage_bucket\nVITE_FIREBASE_MESSAGING_SENDER_ID=seu_sender_id\nVITE_FIREBASE_APP_ID=seu_app_id\n```\n\n7. Reinicie o servidor: `pnpm dev`\n\n---\n\n## ❌ Problema: \"Cannot find module 'drizzle-orm'\"\n\n### Causa\nDependências não foram instaladas.\n\n### Solução\n\n```bash\npnpm install\n```\n\nSe ainda não funcionar:\n\n```bash\nrm -rf node_modules pnpm-lock.yaml\npnpm install\n```\n\n---\n\n## ❌ Problema: \"TypeScript error: Cannot find name 'trpc'\"\n\n### Causa\nArquivos de tipo não foram gerados.\n\n### Solução\n\n```bash\n# Verificar tipos\npnpm check\n\n# Regenerar tipos\npnpm build\n\n# Se ainda não funcionar\nrm -rf dist\npnpm build\n```\n\n---\n\n## ✅ Problema Resolvido?\n\nSe nenhuma solução acima funcionou:\n\n1. **Verifique a documentação:**\n   - `DEVELOPMENT.md` - Desenvolvimento local\n   - `FIREBASE_SETUP.md` - Firebase\n   - `DEPLOYMENT.md` - Vercel\n\n2. **Procure online:**\n   - Google: \"[seu erro] node.js\"\n   - Stack Overflow: [https://stackoverflow.com](https://stackoverflow.com)\n   - GitHub Issues: [https://github.com/search?q=seu+erro](https://github.com/search?q=seu+erro)\n\n3. **Abra uma issue:**\n   - No seu repositório GitHub\n   - Descreva o problema\n   - Cole os logs de erro\n   - Mencione seu sistema operacional\n\n---\n\n**Boa sorte! 🍀**\n

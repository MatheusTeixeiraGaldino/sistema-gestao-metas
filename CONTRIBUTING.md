# Guia de Contribuição\n\nObrigado por considerar contribuir para o Sistema de Gestão de Metas! Este documento fornece diretrizes e instruções para contribuir ao projeto.\n\n## Código de Conduta\n\nEste projeto adota um Código de Conduta para garantir um ambiente acolhedor e inclusivo. Esperamos que todos os contribuidores respeitem este código.\n\n## Como Começar\n\n### 1. Fork o Repositório\n\n```bash\n# Clique em \"Fork\" no GitHub\n# Isso cria uma cópia do repositório na sua conta\n```\n\n### 2. Clone seu Fork\n\n```bash\ngit clone https://github.com/seu-usuario/metas-management-system.git\ncd metas-management-system\n```\n\n### 3. Configure o Remote Upstream\n\n```bash\ngit remote add upstream https://github.com/original-owner/metas-management-system.git\n```\n\n### 4. Instale as Dependências\n\n```bash\npnpm install\n```\n\n## Processo de Desenvolvimento\n\n### Criar uma Branch de Feature\n\n```bash\n# Atualize a branch develop\ngit checkout develop\ngit pull upstream develop\n\n# Crie uma nova branch\ngit checkout -b feature/sua-feature-descritiva\n```\n\n### Nomes de Branches\n\nUse nomes descritivos:\n- `feature/adicionar-dashboard` - Para novas features\n- `fix/corrigir-validacao-metas` - Para correções de bugs\n- `docs/atualizar-readme` - Para documentação\n- `refactor/melhorar-performance` - Para refatoração\n\n### Fazer Commits\n\n```bash\n# Faça commits pequenos e focados\ngit add .\ngit commit -m \"Descrição clara da mudança\"\n\n# Exemplo de boas mensagens de commit:\n# \"Adicionar validação de peso de metas\"\n# \"Corrigir bug no fluxo de aprovação\"\n# \"Refatorar componente de dashboard\"\n```\n\n### Padrão de Commits\n\nUse o seguinte padrão:\n\n```\n<tipo>(<escopo>): <assunto>\n\n<corpo>\n\n<rodapé>\n```\n\n**Tipos:**\n- `feat`: Nova feature\n- `fix`: Correção de bug\n- `docs`: Documentação\n- `style`: Formatação de código\n- `refactor`: Refatoração\n- `test`: Testes\n- `chore`: Tarefas de manutenção\n\n**Exemplo:**\n```\nfeat(goals): adicionar validação de soma de pesos\n\nImplementa validação para garantir que a soma dos pesos\ndas metas de um setor não ultrapasse 100%.\n\nFixes #123\n```\n\n## Antes de Submeter um Pull Request\n\n### 1. Atualize sua Branch\n\n```bash\ngit fetch upstream\ngit rebase upstream/develop\n```\n\n### 2. Execute os Testes\n\n```bash\npnpm test\n```\n\n### 3. Verifique a Formatação\n\n```bash\npnpm format --check\npnpm check  # Type checking\n```\n\n### 4. Build Local\n\n```bash\npnpm build\n```\n\n### 5. Teste Localmente\n\n```bash\npnpm dev\n# Acesse http://localhost:3000 e teste suas mudanças\n```\n\n## Submeter um Pull Request\n\n### 1. Push para seu Fork\n\n```bash\ngit push origin feature/sua-feature-descritiva\n```\n\n### 2. Abra um Pull Request\n\n1. Vá para o repositório original\n2. Clique em \"New Pull Request\"\n3. Selecione sua branch\n4. Preencha o template de PR\n\n### Template de Pull Request\n\n```markdown\n## Descrição\nDescreva as mudanças que você fez.\n\n## Tipo de Mudança\n- [ ] Bug fix\n- [ ] Nova feature\n- [ ] Breaking change\n- [ ] Documentação\n\n## Como foi testado?\nDescreva os testes que você executou.\n\n## Checklist\n- [ ] Meu código segue o estilo do projeto\n- [ ] Executei `pnpm format`\n- [ ] Executei `pnpm test`\n- [ ] Adicionei testes para minhas mudanças\n- [ ] Atualizei a documentação\n- [ ] Não há warnings de build\n\n## Screenshots (se aplicável)\nAdicione screenshots de mudanças na UI.\n\n## Issues Relacionadas\nFixes #123\n```\n\n## Diretrizes de Código\n\n### Estilo de Código\n\n- Use TypeScript para type safety\n- Siga o Prettier para formatação\n- Use ESLint para linting\n- Componentes React em PascalCase\n- Funções e variáveis em camelCase\n- Constantes em UPPER_SNAKE_CASE\n\n### Estrutura de Componentes\n\n```tsx\n// Imports\nimport { useState } from 'react';\nimport { Button } from '@/components/ui/button';\n\n// Types\ninterface MyComponentProps {\n  title: string;\n  onSubmit: (data: string) => void;\n}\n\n// Component\nexport default function MyComponent({ title, onSubmit }: MyComponentProps) {\n  const [state, setState] = useState('');\n\n  return (\n    <div>\n      <h1>{title}</h1>\n      <Button onClick={() => onSubmit(state)}>Submit</Button>\n    </div>\n  );\n}\n```\n\n### Testes\n\nEscreva testes para suas mudanças:\n\n```ts\nimport { describe, it, expect } from 'vitest';\n\ndescribe('MyFeature', () => {\n  it('should do something', () => {\n    expect(true).toBe(true);\n  });\n});\n```\n\n## Adicionando Novas Features\n\n### Checklist de Feature\n\n- [ ] Tabelas/schema adicionadas em `drizzle/schema.ts`\n- [ ] Query helpers adicionados em `server/db.ts`\n- [ ] Procedimentos tRPC adicionados em `server/routers.ts`\n- [ ] Componentes React criados em `client/src/pages/` ou `client/src/components/`\n- [ ] Testes adicionados em `server/*.test.ts`\n- [ ] Documentação atualizada\n- [ ] Migrações executadas: `pnpm db:push`\n\n## Reportando Bugs\n\nAo reportar um bug, inclua:\n\n1. **Descrição clara** do problema\n2. **Passos para reproduzir**\n3. **Comportamento esperado** vs **comportamento atual**\n4. **Screenshots** (se aplicável)\n5. **Ambiente** (OS, navegador, versão Node.js)\n6. **Logs de erro** (se aplicável)\n\n## Sugestões de Features\n\nPara sugerir uma feature:\n\n1. Use o template de \"Feature Request\" no GitHub\n2. Descreva claramente o problema que resolve\n3. Forneça exemplos de uso\n4. Explique por que seria útil\n\n## Perguntas?\n\nSinta-se à vontade para:\n\n- Abrir uma issue com a tag `question`\n- Comentar em PRs existentes\n- Consultar a documentação em `README_SETUP.md`\n\n## Reconhecimento\n\nTodos os contribuidores serão reconhecidos no arquivo `CONTRIBUTORS.md`.\n\n## Licença\n\nAo contribuir, você concorda que suas contribuições serão licenciadas sob a mesma licença MIT do projeto.\n\n---\n\nObrigado por contribuir! 🎉\n
